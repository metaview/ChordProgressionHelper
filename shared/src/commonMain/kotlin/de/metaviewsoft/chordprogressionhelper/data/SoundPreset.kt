package de.metaviewsoft.chordprogressionhelper.data

/** Selectable instrument voice preset. Shared (KMP) so audio + settings code can reference it. */
enum class SoundPreset { CLEAN, OVERDRIVE, PIANO }
