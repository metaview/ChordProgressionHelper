<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/edit_strumming_pattern"
        android:textSize="20sp"
        android:textStyle="bold" />

    <FrameLayout
        android:id="@+id/strumEditorContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp">

        <HorizontalScrollView
            android:id="@+id/strumEditorScroll"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fillViewport="false"
            android:scrollbars="none"
            android:fadingEdge="horizontal"
            android:fadingEdgeLength="@dimen/fade_edge_length"
            android:clipToPadding="false">

            <LinearLayout
                android:id="@+id/strummingPatternEditor"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:paddingLeft="4dp"
                android:paddingRight="4dp"
                android:clipToPadding="false">

                <!-- Icon chips for the strums will be added here dynamically and can scroll horizontally -->

            </LinearLayout>

        </HorizontalScrollView>

        <View
            android:id="@+id/fadeLeft"
            android:layout_width="@dimen/fade_edge_length"
            android:layout_height="wrap_content"
            android:background="@drawable/fade_left"
            android:layout_gravity="start|center_vertical"
            android:clickable="false"
            android:focusable="false" />

        <View
            android:id="@+id/fadeRight"
            android:layout_width="@dimen/fade_edge_length"
            android:layout_height="wrap_content"
            android:background="@drawable/fade_right"
            android:layout_gravity="end|center_vertical"
            android:clickable="false"
            android:focusable="false" />

    </FrameLayout>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:text="@string/default_patterns"
        android:textSize="16sp"
        android:textStyle="bold" />

    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp">

        <LinearLayout
            android:id="@+id/defaultPatternsLayout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <!-- Buttons for default patterns will be added here dynamically -->

        </LinearLayout>
    </HorizontalScrollView>

    <!-- Button bar: Test (left)  |  Cancel + OK (right) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginTop="16dp">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnTest"
            style="@style/Widget.ChordProgressionHelper.MaterialButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/test" />

        <!-- flexible spacer pushes the following buttons to the right -->
        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnCancel"
            style="@style/Widget.ChordProgressionHelper.MaterialButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/cancel"
            android:layout_marginEnd="8dp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnOk"
            style="@style/Widget.ChordProgressionHelper.MaterialButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/ok" />

    </LinearLayout>

</LinearLayout>
